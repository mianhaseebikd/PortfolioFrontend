# Copy to .env.local for local development.
# Netlify: optional — .env.production already has the live API URL for builds.

# Live backend (Railway)
VITE_API_BASE_URL=https://portfoliobackend-production-d4e4.up.railway.app/api

# Local backend (uncomment if testing backend on your machine)
# VITE_API_BASE_URL=http://localhost:5000/api
